[project]
name = "mac-upscaling"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = [
    "torch",
    "torchvision",
    "coremltools>=7.0",
    "Pillow",
    "numpy",
    "tqdm",
    "spandrel>=0.4.2",
    "einops>=0.8.2",
    "safetensors>=0.7.0",
]
